export enum Routes {
    WaitingRoom = '/waiting',
    Questionnaire = '/questionnaire',
    Register = '/register',
    Login = '/login',
}

export enum UserType {
    Member = 'member',
    Proctor = 'proctor',
}
